# Secrets / credentials — never commit
hunter_io_api_key.txt
*.env
.env
*.pem
*.key
*_key.txt
*token*.txt
*credentials*.json
*service-account*.json
*secret*.txt

# n8n workflow exports — can embed live API keys in node parameters
# (e.g. the Serper key in this project's workflow). Keep them out of git.
*.json

# Dependencies
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
